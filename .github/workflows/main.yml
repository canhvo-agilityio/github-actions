name: Auto build the project when push event call
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout my repository
        uses: actions/checkout@v4
      - name: Install node
        uses: actions/setup-node@v4
      - name: Notify Slack on failure
        run: | 
          curl -X POST -H 'Content-type: application/json' --data '{"text":"Hello, World!"}' https://hooks.slack.com/services/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
